<Window x:Class="Lab1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab1"
        mc:Ignorable="d"
        
        Title="MainWindow" Width="1920" Height="1080" WindowState="Maximized" 
        >
    <Window.Resources>
        <local:Converter x:Key="Converter"/>
    </Window.Resources>
    <Grid>

        <Grid Margin="989,10,10,564">
            <ScottPlot:WpfPlot x:Name="CorrelationField" BorderThickness="1" BorderBrush="Black"/>
        </Grid>
        <Button Click="ButtonBase_OnClick" BorderThickness="2" Content="Load data" FontSize="15" HorizontalAlignment="Left" Height="28" Margin="4,3,0,0" VerticalAlignment="Top" Width="107" BorderBrush="DimGray">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="15"></Setter>
                </Style>
            </Button.Resources>
        </Button>
        <Grid Height="300" Margin="10,40,0,0" VerticalAlignment="Top" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="148"></ColumnDefinition>
                <ColumnDefinition Width="390"></ColumnDefinition>
                <ColumnDefinition Width="390"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <DataGrid Grid.Column="0" Margin="0,25,10,0" CanUserResizeColumns="False" CanUserResizeRows="False" x:Name="DataGrid"/>
            <Label  HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Column="1" Content="Для ознаки X"></Label>
            <Label  HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Column="2" Content="Для ознаки Y"></Label>
            <DataGrid Margin="0,25,0,0" CanUserAddRows="False" AutoGenerateColumns="False" Grid.Column="1" Height="140" VerticalAlignment="Top"  x:Name="XStats">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Характеристика"  Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Оцінка"  Binding="{Binding Estimate,Converter={StaticResource Converter}}"/>
                    <DataGridTextColumn Header="Середньоквадратичне відхилення оцінки" Binding="{Binding Deviation,Converter={StaticResource Converter}}"/>
                    <DataGridTextColumn Header="95% довірчий інтервал для характеристики (лівий)"  Binding="{Binding LeftInterval,Converter={StaticResource Converter}}"/>
                    <DataGridTextColumn Header="95% довірчий інтервал для характеристики (правий)"  Binding="{Binding RightInterval,Converter={StaticResource Converter}}"/>
                </DataGrid.Columns>
            </DataGrid>
            <DataGrid CanUserAddRows="False" AutoGenerateColumns="False" Grid.Column="2" Height="140" VerticalAlignment="Top" Margin="10,25,0,0" x:Name="YStats" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Характеристика"  Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Оцінка"  Binding="{Binding Estimate,Converter={StaticResource Converter}}"/>
                    <DataGridTextColumn Header="Середньоквадратичне відхилення оцінки" Binding="{Binding Deviation,Converter={StaticResource Converter}}"/>
                    <DataGridTextColumn Header="95% довірчий інтервал для характеристики (лівий)"  Binding="{Binding LeftInterval,Converter={StaticResource Converter}}"/>
                    <DataGridTextColumn Header="95% довірчий інтервал для характеристики (правий)"  Binding="{Binding RightInterval,Converter={StaticResource Converter}}"/>
                </DataGrid.Columns>
            </DataGrid>
            <Canvas>
                <DataGrid  AutoGenerateColumns="False" CanUserAddRows="False" x:Name="CriteriaGrid" Height="128" Width="780" Canvas.Left="149" Canvas.Top="172">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Критерій"  Binding="{Binding Name}"/>
                        <DataGridTextColumn Header="Оцінка"  Binding="{Binding Estimate,Converter={StaticResource Converter}}"/>
                        
                        <DataGridTextColumn Header="95% довірчий інтервал для характеристики (лівий)"  Binding="{Binding LeftInterval,Converter={StaticResource Converter}}"/>
                        <DataGridTextColumn Header="95% довірчий інтервал для характеристики (правий)"  Binding="{Binding RightInterval,Converter={StaticResource Converter}}"/>
                        <DataGridTextColumn Header="Статистика" Binding="{Binding Statistics,Converter={StaticResource Converter}}"/>
                        <DataGridTextColumn Header="Квантиль" Binding="{Binding Quantile,Converter={StaticResource Converter}}"/>
                        <DataGridTextColumn Header="Висновок 
(значущий/ 
незначущий)" Binding="{Binding Conclusion,Converter={StaticResource Converter}}"/>
                        <DataGridTextColumn Header="Висновок щодо 
наявності 
взаємозв’язку 
(є/немає) " Binding="{Binding RelationConclusion,Converter={StaticResource Converter}}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Canvas>

        </Grid>
    </Grid>
</Window>
